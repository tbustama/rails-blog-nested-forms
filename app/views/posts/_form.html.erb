<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.label :content %><br>
    <%= f.text_area :content %><br>
    Tags:
  </div>

  <div class="field">
    <%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name, include_hidden: false%><br>
  </div>
  <div class="field">
  <%= f.fields_for :tags, @post.tags.build do |attributes|%>
    <div class="field">
    <%= attributes.label :name %>
    <%= attributes.text_field :name %>
    </div>
  <% end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
